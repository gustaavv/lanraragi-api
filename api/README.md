# api

The OpenAPI file is the same to the one in LANraragi, which is located at `tools/openapi.yaml`. Note that the content is not up to date with the one in the `dev` branch, but a specific release tag.